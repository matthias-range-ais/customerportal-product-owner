# frontend

React + TypeScript + Vite UI for customerportal-product-owner. Not run standalone in production — `npm start` at the repo root builds this and serves it from the Fastify backend (`src/api/server.ts`).

See the repo root [README.md](../README.md) and [AGENTS.md](../AGENTS.md) for the full project context and commands.
