---
title: 'Project Scaffold'
type: 'feature' # feature | bugfix | refactor | chore
created: '2026-09-17'
status: 'done' # draft | ready-for-dev | in-progress | in-review | done
route: 'oneshot' # oneshot | dispatch — set by step-02's route gate after design
review_loop_iteration: 0 # incremented by step-04 before each review loopback
context: ['{project-root}/_bmad-output/implementation-artifacts/epic-1-context.md']
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** The repository has planning docs (SPEC, architecture, epics) but zero code — no backend, no frontend, no single start command — so no Epic 1 feature work has anywhere to land.

**Approach:** Scaffold a Fastify 5 (TypeScript) backend and a Vite/React/TypeScript frontend (`npm create vite@latest -- --template react-ts`) inside a Ports & Adapters layout (`src/domain/`, `src/adapters/{equipmentcloud,credentials,storage}/`, `src/api/`), on Node.js 24. One `npm start` builds the frontend and launches the single Fastify process, which serves the built React app and shows a basic landing page.

</frozen-after-approval>

## Implementation Notes

- npm workspaces (`workspaces: ["frontend"]`) chosen over two independent installs, so `npm install` and `npm run build`/`npm start` at the root are the only commands a developer needs.
- Backend uses TypeScript (not just JS) for consistency with the frontend and to type the Ports & Adapters boundary the epic calls for — not explicitly required by the AC but a low-risk, unsurprising default.
- Created empty `src/domain/`, `src/adapters/equipmentcloud/`, `src/adapters/credentials/` (each with `.gitkeep`) to match the epic's stated directory shape, since Stories 1.2–1.5 depend on it existing. `src/adapters/storage/` was left out — not needed until CAP-2.
- Replaced Vite's stock marketing landing page (logos, counter, external links) in `frontend/src/App.tsx` with a minimal page showing the project name, and removed the now-unused template assets (`App.css`, `assets/`, `public/icons.svg`) — satisfies "basic landing page" without carrying dead template code.
- Verified with a clean-state run: deleted `dist/` and `frontend/dist/`, ran `npm start`, confirmed `GET /` returns 200 and serves the built app.
- Updated `AGENTS.md`'s "Running and verifying" section (previously a TODO placeholder) with the real install/build/start commands, since this story is what resolves that TODO.
- Files added/changed: `package.json`, `tsconfig.json`, `.gitignore`, `src/api/server.ts`, `src/domain/.gitkeep`, `src/adapters/equipmentcloud/.gitkeep`, `src/adapters/credentials/.gitkeep`, `frontend/` (Vite scaffold, trimmed), `AGENTS.md`, `docs/sprint-status.yaml` (status sync).
- Post-review fixes: aligned root `typescript` devDependency to `~6.0.2` (was `^7.0.2`, diverging from the frontend's pinned version); translated the landing page's body text to German (`Grundgerüst läuft.`) per AGENTS.md's GUI-text convention; resolved AGENTS.md's SBOM TODO now that `package.json` exists (`npm sbom`); replaced `frontend/README.md`'s generic Vite boilerplate with a project-specific pointer; documented the `frontend/dist` build-output coupling with a comment in `src/api/server.ts`. Re-verified clean-state `npm start` and confirmed the German text reached the built JS bundle after the fixes.
- Deferred (out of this story's AC, logged in `deferred-work.md`): SPA/wildcard fallback routing for future UI views; a root-level lint script covering backend `src/` code.
- Post-walkthrough addition (requested by the user during `bmad-walkthrough` review, since the project had no test framework at all): added Vitest project-wide. Split `src/api/server.ts` into `src/api/app.ts` (exports `buildApp()`, no side effects) and a thin `server.ts` entry point, so the app can be tested via Fastify's `inject()` without binding a real port. Added `src/api/app.test.ts` (asserts `GET /` serves the built HTML shell and that the referenced JS bundle contains the German landing-page text — the same thing manually curl/grep-verified earlier, now automated) and `frontend/src/App.test.tsx` (Testing-Library render test). `npm test` (`pretest` builds the frontend first, since the backend test serves real files from `frontend/dist`) runs both; `frontend`'s own `npm test` runs standalone. Excluded `**/*.test.ts(x)` from both production `tsconfig`s so test files never land in `dist/`.
- Post-walkthrough addition (requested during the Detail Pass risk review, flagging the host as hardcoded): `src/api/server.ts` now reads `HOST` from the environment (defaulting to `127.0.0.1`), mirroring the existing `PORT` handling. Re-verified with `HOST=127.0.0.1 PORT=3001 node dist/api/server.js` and re-ran `npm test` (all green).
- Post-walkthrough fix (surfaced during the Testing step's live error-path check): running the server without a frontend build previously returned a bare `404 {"message":"Route GET:/ not found",...}` with no hint what was wrong. `buildApp()` now checks for `frontend/dist/index.html` upfront and throws a clear `Error` ("run `npm run build` before starting the server") instead; `server.ts` wraps startup in `try/catch` and exits with code 1 on failure. Added a regression test (`src/api/app.test.ts`) that temporarily renames `index.html` away and asserts the rejection; re-verified manually (`node dist/api/server.js` with `frontend/dist` removed now fails fast with the new message and exit code 1) and confirmed the normal path and `npm test` still pass.
- `bmad-code-review` patches applied (10; see Review Triage Log below for full rationale): `frontend/index.html` `lang="de"`; removed dead Vite-template CSS (`.counter`, `#social .button-icon`, `--social-bg`) from `frontend/src/index.css`; documented `npm run dev`'s build prerequisite in `AGENTS.md`; `buildApp()` takes an optional `{ logger }` param (tests now pass `logger: false` to stop `npm test` log spam); aligned `@types/node` to `^24.13.5` across both `package.json`s; installed `@testing-library/jest-dom` and replaced `App.test.tsx`'s no-op `.toBeDefined()` with real `.toBeInTheDocument()` assertions; added `engines.npm` (`>=11`, needed for the `allowScripts` gate); extracted `envOrDefault()` into `src/api/env.ts` (unit-tested) and used it for both `PORT`/`HOST` so an empty-string env value no longer slips past `??` — empirically confirmed `HOST=""` previously bound to all interfaces (`::`) instead of `127.0.0.1`, a real NFR9 violation, now fixed; created the missing `src/adapters/storage/.gitkeep` (the frozen Intent always listed it; the earlier "can't touch frozen text" rejection was itself wrong — creating the folder doesn't edit that text); added `src/api/server.test.ts` (child-process tests via `tsx`, covering the fail-fast exit code and the HOST/PORT-driven successful bind — `server.ts`'s own top-level logic was previously untested, only `buildApp()` was). Discovered and fixed a real test-isolation bug while adding these: `app.test.ts` and `server.test.ts` both rename the same real `frontend/dist/index.html`, which raced when Vitest ran the two files in parallel workers; set `fileParallelism: false` in `vitest.config.ts` to fix it. Re-verified: full clean build, `npm test` green twice in a row (9 backend + 1 frontend tests), manual `npm start` and `HOST="" PORT=3002 node dist/api/server.js` (now correctly logs `listening at http://127.0.0.1:3002`, not `::`).

## Review Triage Log

- **medium** — root `package.json` pinned `typescript@^7.0.2` while `frontend/package.json` pins `~6.0.2`, two major versions apart in one repo. Confirmed by reading both files. Patched: aligned root to `~6.0.2`; rebuilt and re-verified clean-state `npm start`.
- **low** — landing page text ("Scaffold running.") was English, violating AGENTS.md's "GUI-facing text: German" convention. Confirmed by reading `frontend/src/App.tsx` against `AGENTS.md`. Patched: translated to "Grundgerüst läuft."; confirmed the German string reached the built JS bundle.
- **low** — AGENTS.md's SBOM line was still "TODO once `package.json` exists," but this changeset creates `package.json`. Confirmed by reading the file. Patched: replaced with the concrete command (`npm sbom`), already specified as NFR7 in `docs/epics.md`.
- **low** — `frontend/README.md` was untouched Vite template boilerplate with no project context. Confirmed by reading the file. Patched: replaced with a short project-specific pointer to the root README/AGENTS.md.
- **low** — the `frontendDist` path in `src/api/server.ts` implicitly depends on Vite's default `dist` output directory, undocumented. Confirmed by reading both files (`vite.config.ts` sets no `build.outDir`). Patched: added a one-line comment noting the coupling.
- **false** — reviewer claimed `allowScripts` in root `package.json` is "not a field npm itself reads" (a pnpm-only concept) and is dead configuration. Disproved: this session's npm (11.19.0) natively provides `npm install-scripts approve/deny/ls`, which wrote this exact field, and the gated package (`esbuild`) installed and ran correctly afterward — confirmed by the build and server tests succeeding.
- **false** — reviewer claimed `docs/sprint-status.yaml` and the spec's own status were left lagging behind finished work. Disproved: this Finalize Spec step (status → `done`, sprint-status → `review` via `sync-sprint-status.md`) runs immediately after classification, per the oneshot workflow's own ordering — not a gap in the delivered work.
- **low, rejected** — reviewer noted the frozen Intent's approach text lists `src/adapters/{equipmentcloud,credentials,storage}/`, but `storage/` wasn't created. Real, but the fix (editing frozen text) is against this workflow's rules once written; the deviation is already documented in Implementation Notes, which is the correct channel for it. Not re-opened.
- **maybe-false, deferred** — no SPA/wildcard fallback for future client-side routes. Real gap, but explicitly out of Story 1.1's AC (one static landing page only); would become relevant starting with Stories 1.4/1.5's real UI views. Logged in `deferred-work.md`.
- **maybe-false, deferred** — no root-level lint script for backend `src/` code. Real gap, but out of Story 1.1's AC and already tracked as an open TODO in `AGENTS.md`. Logged in `deferred-work.md`.

### bmad-code-review round (2026-09-17, commit range `fb53ffd..HEAD`)

Four adversarial reviewers (Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor) ran against the diff (excluding `package-lock.json` — generated, not reviewed). 19 grouped findings after triage: 0 decision-needed, 10 patch, 3 defer, 6 rejected.

- **medium** — `frontend/index.html` declares `lang="en"` while all rendered GUI text is German (AGENTS.md convention). Confirmed by reading the file. Patch.
- **low** — `frontend/src/index.css` still has dead Vite-template rules (`.counter`, `#social .button-icon`) for markup removed from `App.tsx`, contradicting this file's own claim that unused template assets were fully removed. Confirmed by reading the file. Patch.
- **low** — `npm run dev` (`tsx watch`) fails on first run if `npm run build` was never run — the new fail-fast error is clear, but AGENTS.md's Dev line doesn't mention the prerequisite. Confirmed by running `npx tsx src/api/server.ts` with no prior frontend build (exits 1 with the fail-fast message). Patch: doc-only.
- **low** — `Fastify({ logger: true })` is unconditional, so every `npm test` run prints Fastify's JSON request logs. Confirmed by reading test output. Patch.
- **low** — `@types/node` drifts between root (`^24.13.5`) and frontend (`^24.13.3`), the same class of issue already patched for `typescript`. Confirmed by reading both `package.json`s. Patch.
- **low** — `frontend/src/App.test.tsx` asserts `.toBeDefined()` on a `getByText()` result, which is a no-op (`getByText` already throws if not found). Confirmed by reading the test. Patch.
- **low** — root `package.json` `engines` pins `node` but no `npm` minimum, despite relying on npm-specific behavior (workspaces, `allowScripts`). Confirmed by reading the file; fix is a one-line addition. Patch.
- **medium** — `PORT`/`HOST` parsing uses `??`, which does not catch an empty-string env value. Confirmed empirically: `Number('') === 0` (binds a random port instead of 3000), and a raw Node `net.Server` with `host: ''` bound to `::` (all interfaces) — a real violation of NFR9's local-only requirement if `HOST` is ever set to an empty string. Patch.
- **medium** — `src/adapters/storage/` is missing even though the frozen Intent's Approach text lists it as part of the layout. Correction to this log's own earlier entry: the previous "low, rejected" verdict reasoned that fixing this would require editing frozen text — that's wrong; creating the empty directory doesn't touch the frozen Intent text at all, it just fulfills what the text already says. Patch: create `src/adapters/storage/.gitkeep`.
- **medium** — `src/api/server.ts`'s own logic (HOST/PORT resolution, the try/catch fail-fast exit) is never executed by any test — only `buildApp()` is tested via `inject()`. Verification-Gap reviewer confirmed no test imports `server.ts` or exercises `process.exit`/`app.listen`. Patch: extract a testable function and/or add a child-process test.
- **false** — reviewer claimed no `package-lock.json` is part of this change. Disproved: it exists and is committed (in `fca72d0`); it was deliberately excluded from the *reviewed diff file* for size/noise reasons, not missing from the repo.
- **false** — reviewer claimed the `allowScripts` exact-version pin (`esbuild@0.28.2`) would silently block a future esbuild version bump. Disproved: this session observed npm print an explicit `npm warn install-scripts ...` when a package's script isn't covered — not a silent block.
- **false** — reviewer claimed the installed Fastify/React versions "deviate" from `epic-1-context.md`'s stated versions. Disproved: caret ranges resolving to the actual current published version is correct dependency-resolution behavior, not a deviation; the context doc's versions were a snapshot at writing time, not hard pins.
- **low, rejected** — no graceful shutdown (SIGINT/SIGTERM → `app.close()`). No demonstrated harm for this local, single-user, single-process tool; fix would add signal-handling complexity with no shown benefit yet.
- **low, rejected** — the fail-fast check only verifies `frontend/dist/index.html` exists, not every asset it references. Very unlikely partial-corruption scenario; fix (per-asset validation or a `NotFoundHandler` redesign) is more than a direct correction.
- **low, rejected** — `app.test.ts`'s rename-based fail-fast test could throw `ENOENT`/`EBUSY` if `index.html` is already missing from a crashed prior run. Very unlikely in practice; fix adds test-only defensive complexity for a self-inflicted scenario.
- **maybe-false, deferred** — no CI pipeline runs `npm test`/`npm run build` on push. Real gap, pre-existing (this diff is what first made a test suite exist to run), and a larger infra decision than this story's scope. Logged in `deferred-work.md`.
- **maybe-false, deferred** — no SPA/wildcard fallback for future client-side routes. Same gap already logged in `deferred-work.md` from the earlier oneshot review; re-confirmed, not duplicated.
- **maybe-false, deferred** — this spec's own Implementation Notes says the `frontend/dist` coupling comment lives in `src/api/server.ts`; it's actually in `src/api/app.ts` (moved there when `server.ts` was split). Real inaccuracy, but the fix edits this spec file itself. Logged in `deferred-work.md`.

